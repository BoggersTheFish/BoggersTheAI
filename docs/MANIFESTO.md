# TS-OS Manifesto — Cognitive Physics for Verifier-First Reasoning

> **Thought is not token prediction. Thought is the stable configuration of
> constraints under wave propagation.**

This document is the unified ideological deployment surface for BoggersTheAI.
Every researcher interacting with the system must engage with these epistemological
rules before contributing code, running experiments, or citing results.

---

## Core Axiom

Language models **propose**. Typed verifier support **decides**. Confidence is not
proof. The proof boundary is the claim verifier, not the softmax output.

```
Substrates expose tension → Models propose → TS verifies → Receipts decide
```

## The Propagate → Relax → Break → Evolve Cycle

All TS-OS layers share one computational rhythm:

1. **Propagate** — activation and tension flow through the constraint graph
2. **Relax** — attractor dynamics settle toward stable configurations
3. **Break** — high-tension states trigger directional escapes (not random jitter)
4. **Evolve** — emergent structure spawns when verifier support is typed

This cycle runs in the living graph (BoggersTheAI runtime), in TensionLM's
sigmoid tension fields (no zero-sum competition), and in BOGVM-0's deterministic
16-opcode wave-state machine.

## Proof Against Token Prediction

| Token Prediction | TS-OS Cognitive Physics |
|------------------|-------------------------|
| Next-token softmax competition | Independent sigmoid tension per pair |
| Stateless context window | Persistent living graph with activation/stability |
| Confidence = probability mass | Confidence ≠ proof; verifier receipts decide |
| Black-box weights | `.bogpk` artifact trails — reproducible, compressed |
| Scale parameters | Scale constraints and verifier gates |

## The Three-Layer Logic Map

```
core-vm/     BOGVM-0 bedrock — 16-opcode deterministic wave-state VM
inference/   TensionLM + TensionForge — mathematical parity, neural execution
reasoner/    GOAT-TS + Verse Engine — constraint resolution, graph hooks
```

Data moves between layers **only** through `.bogpk` binary containers. Whether
BOGVM-0 logs a state change or TensionLM outputs a tension field, everything
serializes through the highly compressed format to maintain a perfect,
reproducible artifact trail.

## Epistemological Rules

1. **No substrate accepts its own output.** Verifier support is always external.
2. **Receipts are proof authority.** Containers (`.bog`, `.bogpk`) are reconstruction blueprints, not proof.
3. **Simulators are bounded.** Photonic ledgers, retrocausal fuzzers, and lazy-universe engines are deterministic contracts — not claims of literal hardware.
4. **Archive satellites stay frozen.** Development happens in this monorepo only.
5. **Artifact trails are mandatory.** Any state change without a `.bogpk` receipt is untrusted.

## Claim Boundary

52 historic repositories were merged into this monorepo. Four active satellites
are now archived with redirect notices:

- BoggersTheMind → `interface/`, `mind/`, `entities/`
- BoggersTheLLM → `inference/` (language substrate)
- bozo (TensionLM) → `inference/tension_lm/`
- TS-Reasoner-v0 → `reasoner/`

## For Researchers

Read in order:

1. [Truth Graph Mechanics](truth-graph.md)
2. [BOGPK Pipeline](bogpk-pipeline.md)
3. [TS-OS Directory Structure](directory-structure.md)
4. [Proof Against Token Prediction](anti-token-prediction.md)

---

*Unified manifesto — BoggersTheAI monorepo, June 2026.*